<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp">


    <ImageView
        android:id="@+id/noVehicleImage"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:src="@drawable/no_vehicle"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toTopOf="parent"/>

    <TextView
        android:id="@+id/noVehicleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/empty_garage"
        android:textAlignment="center"
        app:layout_constraintTop_toBottomOf="@id/noVehicleImage"
        app:layout_constraintLeft_toLeftOf="@id/noVehicleImage"
        app:layout_constraintRight_toRightOf="@id/noVehicleImage"/>


    <Button
        android:id="@+id/addVehicleButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        android:text="@string/add_vehicle" />

</android.support.constraint.ConstraintLayout>